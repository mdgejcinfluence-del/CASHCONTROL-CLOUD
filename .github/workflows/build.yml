import os

# Chemin vers le fichier problématique
gradle_path = f"/content/{PROJECT_NAME}/android/app/build.gradle.kts"

gradle_fix_content = """
import java.util.Properties

plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin")
}

android {
    namespace = "com.ponium.cashcontrol.cash_control_cloud"
    compileSdk = flutter.compileSdkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.ponium.cashcontrol.cash_control_cloud"
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode()
        versionName = flutter.versionName()
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.getByName("debug")
        }
    }
}

flutter {
    source = "../.."
}
"""

with open(gradle_path, "w") as f:
    f.write(gradle_fix_content)

# Envoi de la correction vers GitHub
%cd /content/{PROJECT_NAME}
!git add android/app/build.gradle.kts
!git commit -m "🔧 FIX: Android Gradle Kotlin syntax (versionCode/Name functions)"
!git push origin main

print("\\n✅ Correction de la configuration Android envoyée !")
