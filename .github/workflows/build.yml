name: CashControl Cloud Auto-Build
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v4
      
      - name: ☕ Configuration de Java (LTS 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - name: 🚀 Configuration de Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0' 
          cache: true
          
      - name: 🧹 Nettoyage et Préparation
        run: |
          flutter clean
          flutter pub get
        
      - name: 🛠 Compilation de l'APK (Release)
        # On ajoute des flags pour stabiliser le build Gradle
        run: flutter build apk --release --no-pub --debug-info
        
      - name: 📤 Livraison de l'APK
        uses: actions/upload-artifact@v4
        with:
          name: CashControl-Cloud-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
